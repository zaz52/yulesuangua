# ===== 自动生成：pip freeze 真实安装版本 =====
# 由 pip freeze > requirements.txt 生成，仅保留项目直接使用的包
# 生成日期: 2026-04-15

# ===== 核心框架 =====
fastapi==0.117.1
uvicorn==0.36.0
pydantic==2.11.9
python-multipart==0.0.20

# ===== LLM 依赖 =====
langchain-core==1.0.2
langchain-openai==1.0.1
openai==2.30.0
httpx==0.28.1

# ===== 奇门排盘 =====
lunar-python==1.4.8

# ===== 沙箱 SDK（外部部署时可选） =====
# 在 Render 等外部平台部署时，如果安装失败可注释掉此行
# 设置 LLM_API_KEY 即可走 OpenAI 兼容模式，不需要此 SDK
coze-coding-dev-sdk==0.5.14

# ===== SDK 间接依赖（部分环境需显式声明） =====
beautifulsoup4==4.12.2
soupsieve==2.8.3
requests==2.33.1
